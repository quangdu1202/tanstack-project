import { Button } from '../Button';
import { IconMaximize } from '@tabler/icons-react';

export const Fullscreen = () => {
  return (
    <Button label={'Fullscreen'} onClick={() => {}}>
      <IconMaximize size={20} />
    </Button>
  );
};
